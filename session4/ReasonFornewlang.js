function check(value)
{
    // if(value)
    if(value==true)
    {
        console.log("perform a Action")
    }
    else
    {
        console.log("can't perform anythin")
    }
}

// check(true)
check("true")

